#include "main.h"

/**
 * _abs - computes th absolute value of an integer 
 * @n: integer to be computed
 * Return: the absolute value
 */
int _abs(int)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
